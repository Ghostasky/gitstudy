信息收集：

-   敏感目录
-   敏感备份文件
-   banner识别



# 敏感目录泄露

## git泄露

### 1.常规git泄露

推荐工具：https://github.com/denny0223/scrabble





### 2.git回滚

HEAD表示当前版本，HEAD^表示上一个版本

`git reset --hard HEAD^`

### 3.git分支



# 敏感备份文件

## getdit备份文件

用gedit编辑后，会生成一个以~结尾的文件

## vim备份文件

vim第一次以外退出：\*.swp

第二次：\*swo 

.....

# bannner识别

指纹识别

----------------

# 命令执行漏洞

转义字符：

-   win下为：^
-   Linux下为：\

在linux下双括号包裹的字符串:\$()或者反引号中的内容也可以被当做命令执行



注释符号：

-   win下为：::
-   Linux下为：#

## 命令执行的绕过和技巧

## 1.缺少空格

有时php会替换空格：

str_replace(" ","",\$_GET['aa'])

但是在命令中间隔的字符可以不只是空格(%20)，也可以是其他字符：%09,%0b,%0c等

可以使用burp进行fuzz测试

-   win下：

    比如echo %ProgramFiles%

    echo%ProgramFiles:~10,1%111

    以上命令表示从第十个开始且获取一个字符串，就是空格

-   Linux下：

    